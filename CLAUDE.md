# AI Instructions

Read and follow all instructions in `INSTRUCTIONS.md` in this same directory.

At the start of every conversation:
1. Read `INSTRUCTIONS.md` for project rules, git workflow, and session protocol
2. Follow the Session Protocol defined there (read docs/CONTEXT.md, docs/SESSION-LOG.md, etc.)
3. When the session ends, follow the ending protocol (update session log, context, decisions)
4. For frontend styling work, also read `frontend/DESIGN_GUIDE.md` and use design tokens/classes instead of hardcoded hex colors
