Install editable from repo root:

```bash
pip install -e ml_service
```

Import in notebooks or scripts:

```python
from ml_service.features.donor_churn import build_training_panel, load_training_frames, train_donor_churn_rf
```

See `docs/ml-deployment.md` for training and deployment.
