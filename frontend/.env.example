# Optional. If unset in dev, requests use /api and Vite proxies to http://localhost:5176 (default `dotnet run` http profile).
# Use this when you need a full URL (e.g. https profile): must match the URL where the API actually listens.
# VITE_API_BASE_URL=http://localhost:5176
# VITE_API_BASE_URL=https://localhost:7200
